# requirements.txt
streamlit>=1.30.0
pocketflow>=0.0.2
openai>=1.3.0
# sqlite3 is part of Python's standard library, no separate install needed
# python-dotenv might be useful for local dev outside Docker but not strictly needed for Dockerized app if using -e